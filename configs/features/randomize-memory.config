# Hardening Feature: CONFIG_RANDOMIZE_MEMORY (Randomize Kernel Memory Sections)
# Randomizes the base virtual address of direct physical memory mapping, vmalloc, and vmemmap
CONFIG_RANDOMIZE_BASE=y
CONFIG_RANDOMIZE_MEMORY=y
CONFIG_RANDOMIZE_MEMORY_PHYSICAL_PADDING=0xa

# Baseline: KASLR enabled but direct memory mapping randomization disabled
# Direct physical memory mapping is deterministic at 0xffff888000000000 (x86_64)
CONFIG_RANDOMIZE_BASE=y
# CONFIG_RANDOMIZE_MEMORY is not set
